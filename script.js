const = document.getElementId ("Submit")
const btn = document.querySelector(".button");
let load;

btn.classList.add("click");
 btn.classList.remove("click");


function myFunction(){
    load = setTimeout(alertFunction, 1000);
}

function alertFunction(){
    if (button.value  == "click"){
        button.value !== ''
}

}
